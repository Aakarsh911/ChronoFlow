version: "2"
sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./queries"
    gen:
      go:
        package: "repos"
        out: "../api/internal/repos"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: false
        overrides: []
